# Frame Transformer Picker Source

Independent Frame Transformer picker package containing preprocessing and
sample construction, model and dataset definitions, training, continuous
inference, and positive-window inference. It can run alone or as one member of
an AI-PAL ensemble. Executable workflows and editable configs live in
`../2_train_picker/` and `../3_run_ai_pal/`.
