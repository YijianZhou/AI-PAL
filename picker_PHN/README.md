# PhaseNet Picker Source

AI-PAL PhaseNet model structure, dataset, training loop, and continuous/positive
inference. This is separate from the reference SeisBench PhaseNet branch used by
the realtime workflow. Executable workflows live in `../2_train_picker/` and
`../3_run_ai_pal/`.